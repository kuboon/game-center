{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ga-cen.kbn.one/schema/gamecenter.json",
  "title": "gamecenter.json",
  "description": "A game and its achievements, as game-center registers them. Embed it in the game's page inside <script type=\"application/gamecenter+json\">, or serve it as gamecenter.json beside the page.",
  "type": "object",
  "required": [
    "id",
    "author",
    "title",
    "achievements"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Points editors at this schema."
    },
    "id": {
      "type": "string",
      "description": "The game's slug, unique among this author's games rather than globally. Its full name is {author}/{id}.",
      "pattern": "^[a-z0-9][a-z0-9-]{1,62}[a-z0-9]$"
    },
    "author": {
      "type": "string",
      "description": "The author's game-center handle, shown on the hub's /me page. The named account approves the registration, which is what completes it.",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,62}[A-Za-z0-9]$"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    },
    "description": {
      "type": "string",
      "maxLength": 500
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "Where the game is played. Optional: a manifest the hub fetched is already at the game's URL. Required only when the manifest is pasted into the dashboard. https, or http for localhost."
    },
    "icon": {
      "type": "string",
      "format": "uri",
      "description": "Icon URL. May be relative to the manifest's location."
    },
    "achievements": {
      "type": "array",
      "maxItems": 200,
      "items": {
        "type": "object",
        "required": [
          "key",
          "title"
        ],
        "additionalProperties": false,
        "properties": {
          "key": {
            "type": "string",
            "description": "Unique within the game. What unlock() names.",
            "pattern": "^[a-z0-9][a-z0-9_-]{0,62}[a-z0-9]$"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100
          },
          "description": {
            "type": "string",
            "maxLength": 500
          },
          "points": {
            "type": "integer",
            "minimum": 0,
            "default": 0
          },
          "hidden": {
            "type": "boolean",
            "default": false,
            "description": "Hide the title and description until it is unlocked."
          }
        }
      }
    }
  }
}
